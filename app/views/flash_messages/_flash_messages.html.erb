<% # loop through and render any flash messages %>

<% flash.each do | type, message | %>
  <% unless type == 'title' %>
    <%= render "flash_messages/flash_" + type, title: flash[:title], message: message %>
  <% end %>
<% end %>
